# dsh-question-jump-bar bundle patch: inserts the conversation-interaction
# row into a profile's layer stack. The browser half registers the quote
# panel / edit-and-resend / custom user renderer; the host half is a
# no-op placeholder so the row activates.
- insert:
    - id: question-jump-bar
      name: 'dsh-question-jump-bar'
